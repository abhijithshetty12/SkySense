# API Configuration
# The URL of the backend API server
# Default: http://localhost:8000 (standard FastAPI port)
VITE_API_URL=http://localhost:8000

# Set to 'true' to use mock data instead of calling the backend API
# Useful for development or when backend is not available
# Default: false
VITE_USE_MOCK_DATA=false
